diff --git b/vendor/dbuenzli-astring/dune-project a/vendor/dbuenzli-astring/dune-project
new file mode 100644
index 0000000..42c0c16
--- /dev/null
+++ a/vendor/dbuenzli-astring/dune-project
@@ -0,0 +1 @@
+(lang dune 1.10)
diff --git b/vendor/dbuenzli-astring/src/astring_escape.ml a/vendor/dbuenzli-astring/src/astring_escape.ml
index adff258..3e75a3d 100644
--- b/vendor/dbuenzli-astring/src/astring_escape.ml
+++ a/vendor/dbuenzli-astring/src/astring_escape.ml
@@ -138,7 +138,7 @@ let unescaped_len ~ocaml s =    (* derives length and checks syntax validity *)
         then None (* invalid escape *)
         else loop (i + 1) (l + 1)
     | ('b' | 't' | 'n' | 'r' | '"' | '\'') when ocaml -> loop (i + 1) (l + 1)
-    | c -> None (* invalid escape *)
+    | _c -> None (* invalid escape *)
   in
   loop 0 0
 
@@ -168,7 +168,7 @@ let _unescape ~ocaml s = match unescaped_len ~ocaml s with
       | 'r' -> bytes_unsafe_set b k '\r'; loop (i + 1) (k + 1)
       | '"' -> bytes_unsafe_set b k '\"'; loop (i + 1) (k + 1)
       | '\'' -> bytes_unsafe_set b k '\''; loop (i + 1) (k + 1)
-      | c -> assert false (* because of unescaped_len  *)
+      | _c -> assert false (* because of unescaped_len  *)
     in
     loop 0 0
 
diff --git b/vendor/dbuenzli-astring/src/dune a/vendor/dbuenzli-astring/src/dune
new file mode 100644
index 0000000..5f2dc57
--- /dev/null
+++ a/vendor/dbuenzli-astring/src/dune
@@ -0,0 +1,11 @@
+(library
+  (name astring)
+  (public_name astring)
+  (modules
+    astring
+    astring_string
+    astring_char
+    astring_base
+    astring_unsafe
+    astring_escape
+    astring_sub))
